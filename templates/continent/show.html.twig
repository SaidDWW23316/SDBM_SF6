{% extends 'base.html.twig' %}

{% block title %}Continent{% endblock %}

{% block body %}
{% block header %}{% endblock %}
    <h1 class='my-5'>Continent</h1>
    <a href="{{ path('app_continent_index') }}">
        <button class='btn btn-primary'>Retour à la liste</button>
    </a><br><br>
    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ continent.id }}</td>
            </tr>
            <tr>
                <th>Continent</th>
                <td>{{ continent.nomContinent }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('continent/_delete_form.html.twig') }}

    <br><a href="{{ path('app_continent_index') }}">
        <button class='btn btn-primary'>Retour à la liste</button>
    </a>
    <br><br>
    <!--<a href="{{ path('app_continent_edit', {'id': continent.id}) }}">
        <button class='btn btn-success'>Edit</button>
    </a><br><br>-->

    
{% endblock %}
