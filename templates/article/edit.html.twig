{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1 class='my-5'>Editer Article</h1>
    
    {# {{ include('continent/_form.html.twig', {'button_label': 'Modifier'}) }} #}
    {{ form_start(form) }}
    <a class='btn btn-primary btn-sm mb-3' href="{{ path('app_article_index') }}">Retour à la liste</a>
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button><br>
    <a class='btn btn-primary btn-sm mt-3' href="{{ path('app_article_index') }}">Retour à la liste</a>
{{ form_end(form) }}

    

    <!--{{ include('article/_delete_form.html.twig') }}-->
{% endblock %}