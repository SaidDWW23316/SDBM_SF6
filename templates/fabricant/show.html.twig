{% extends 'base.html.twig' %}

{% block title %}fabricant{% endblock %}

{% block body %}
{% block header %}{% endblock %}
    <h1 class='my-5'>Fabricant</h1>
    <a href="{{ path('app_fabricant_index') }}">
        <button class='btn btn-primary btn-sm'>Retour à la liste</button>
    </a><br><br>
    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ fabricant.id }}</td>
            </tr>
            <tr>
                <th>Fabricant</th>
                <td>{{ fabricant.nomFabricant }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('fabricant/_delete_form.html.twig') }}

    <br><a href="{{ path('app_fabricant_index') }}">
        <button class='btn btn-primary btn-sm'>Retour à la liste</button>
    </a>
    <br><br>
    <!--<a href="{{ path('app_fabricant_edit', {'id': fabricant.id}) }}">
        <button class='btn btn-success'>Edit</button>
    </a><br><br>-->

    
{% endblock %}
