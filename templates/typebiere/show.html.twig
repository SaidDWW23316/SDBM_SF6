{% extends 'base.html.twig' %}

{% block title %}Couleur{% endblock %}

{% block body %}
{% block header %}{% endblock %}
    <h1 class='my-5'>Couleur</h1>
    <a href="{{ path('app_couleur_index') }}">
        <button class='btn btn-primary btn-sm'>Retour à la liste</button>
    </a><br><br>
    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ couleur.nomCouleur }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('couleur/_delete_form.html.twig') }}

    <br><a href="{{ path('app_couleur_index') }}">
        <button class='btn btn-primary btn-sm'>Retour à la liste</button>
    </a>
    <br><br>
    <!--<a href="{{ path('app_couleur_edit', {'id': couleur.id}) }}">
        <button class='btn btn-success'>Edit</button>
    </a><br><br>-->

    
{% endblock %}
