{% extends 'base.html.twig' %}

{% block title %}Type Bière{% endblock %}

{% block body %}
{% block header %}{% endblock %}
    <h1 class='my-5'>Type de Bière</h1>
    <a href="{{ path('app_typebiere_index') }}">
        <button class='btn btn-primary btn-sm'>Retour à la liste</button>
    </a><br><br>
    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ typebiere.id }}</td>
            </tr>
            <tr>
                <th>typebiere</th>
                <td>{{ typebiere.nomTypebiere }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('typebiere/_delete_form.html.twig') }}

    <br><a href="{{ path('app_typebiere_index') }}">
        <button class='btn btn-primary btn-sm'>Retour à la liste</button>
    </a>
    <br><br>
    <!--<a href="{{ path('app_typebiere_edit', {'id': typebiere.id}) }}">
        <button class='btn btn-success'>Edit</button>
    </a><br><br>-->

    
{% endblock %}
